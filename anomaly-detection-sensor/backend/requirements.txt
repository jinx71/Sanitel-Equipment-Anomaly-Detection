fastapi>=0.110
uvicorn[standard]>=0.29
scikit-learn>=1.3
numpy>=1.26
pandas>=2.1
pydantic>=2.6
python-dotenv>=1.0

# Optional: better 2D separation in the latent-space map. Pulls in numba, so it
# is left out of the default install. The /api/analyze projection falls back to
# PCA automatically when UMAP is not present.
# umap-learn>=0.5
